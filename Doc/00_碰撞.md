# UE5碰撞配置
## 对象类型
Collision Responses（通用碰撞响应）
- Trace Responses（追踪响应，如射线检测）
- Object Responses（对象类型响应）
每一行代表一个对象或系统类型，列则表示三种可能的响应方式：

## 响应类型
Ignore	忽略碰撞/追踪 —— 不产生任何反应
Overlap	触发重叠事件（OnComponentBeginOverlap / OnComponentEndOverlap）—— 可以感知接触但不阻止移动
Block	阻止移动并触发物理碰撞（如反弹、停止）—— 物理上阻挡